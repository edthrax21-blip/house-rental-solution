# Optional: set when the API runs on a different URL than the Vite proxy target.
# The proxy (vite.config.ts) forwards /api to http://localhost:5000 by default.
# If your API runs on another port (e.g. when debugging in Visual Studio), set:
# VITE_API_URL=http://localhost:5000
# or whatever URL your API uses (e.g. http://localhost:5001).
